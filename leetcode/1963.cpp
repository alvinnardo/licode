#include "../template/head.h"
#include <bits/stdc++.h>

int minSwaps(string s) {
    // 思路

    return 0;
}

int main(int argc, char *argv[]) {
    // 1
    cout << minSwaps("][][") << endl;
    // 2
    cout << minSwaps("]]][[[") << endl;
    // 0
    cout << minSwaps("[]") << endl;

    return 0;
}
